{
  "name": "trading-platform-server",
  "version": "5.0.0",
  "description": "Trading Platform - Deployed on Render",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "cd frontend && npm run build && cd .. && cp -r frontend/dist/* public/"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "cors": "^2.8.6",
    "express": "^5.2.1",
    "express-rate-limit": "^8.5.2",
    "helmet": "^8.2.0",
    "jsonwebtoken": "^9.0.3",
    "uuid": "^14.0.0",
    "pg": "^8.16.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
